'use strict';

describe('videoSystem.displayVideos module', function() {

  beforeEach(module('videoSystem.displayVideos'));

  describe('displayVideos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('displayVideosController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});