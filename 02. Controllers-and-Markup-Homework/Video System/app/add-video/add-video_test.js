'use strict';

describe('videoSystem.addVideo module', function() {

  beforeEach(module('videoSystem.addVideo'));

  describe('addVideo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('addVideoController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});